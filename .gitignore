# These files will be ignored, we need this file so git dosent look for theses during your git related commands
# for i in range(100):  'I will not import the files that should be kept secret !'

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
